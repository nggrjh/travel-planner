version: "3"

includes:
  docker: docker.yml
  migrate: db/migrate.yml

vars:
  DB_DOCKER_NAME: pgtest
  DB_DOCKER_PASS: pgtest
  DB_SCHEMA: pgtest
  DB_DIR: ./db

tasks:

  test:
    dir: db
    deps:
      - docker:cleanup
    cmds:
      - docker-compose down
      - docker-compose up -d
      # - docker-compose run wait --rm
      # - docker-compose run migration
